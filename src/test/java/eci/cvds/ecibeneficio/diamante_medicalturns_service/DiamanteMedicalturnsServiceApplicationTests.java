package eci.cvds.ecibeneficio.diamante_medicalturns_service;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class DiamanteMedicalturnsServiceApplicationTests {
  @Test
  void contextLoads() {
    // This method is empty because its purpose is simply to verify that
    // the application context loads correctly without errors.
    // If the application starts without issues, the test passes. No specific
    // actions are required in this case.
  }
}
